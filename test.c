#include "stdio.h"
int main()
{
	
	printf("%.*f",2,1.24443);

return 0;
}
























/* 
   #define N 5 int main() {

   char ch[N][N] = { "0" }; int i, j; for (i = 0; i < 2; i++) { scanf("%s",
   ch[i]); printf("\n\n刚刚输入了%s\n", ch[i]); }
   printf("这个数组是[%d][%d]\n", N, N); for (i = 0; i < N; i++)
   printf("%d\t", i);

   printf("\n"); for (i = 0; i < N; i++) { for (j = 0; j < N; j++)
   printf("%c\t", ch[i][j]);

   printf("\n", i); } printf("程序结束"); return 0; } */
